apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "labeling.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "labeling.name" . }}
    helm.sh/chart: {{ include "labeling.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "labeling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
